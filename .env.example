# Copy to .env for podman compose / docker compose (same directory as compose.yaml)

# Listen address — use 127.0.0.1 when using Caddy (https profile); 0.0.0.0 for direct 8080 only.
# UVICORN_HOST=0.0.0.0
# UVICORN_PORT=8080

# HTTPS via Caddy (docker compose --profile https): public DNS name for this host.
# CADDY_DOMAIN=ddns.example.com

BIND_KEY_API_AUTH_TOKEN=change-me-to-a-long-random-secret-min-16-chars

# Paths and zone — must match your BIND layout (examples only; adjust for your server).
BIND_KEY_API_BIND_KEYS_PATH=/etc/named.dyn.redhatworkshops.io.key
BIND_KEY_API_ZONE_NAME=dyn.redhatworkshops.io
BIND_KEY_API_ZONE_FILE_PATH=/var/named/dyn.redhatworkshops.io.zone

# Delete client DNS names when removing a TSIG key (requires BIND_KEY_API_ZONE_NAME).
# Set false if you only want to drop the key from the keyfile and fix DNS yourself.
BIND_KEY_API_DELETE_ZONE_RRSETS_ON_KEY_DELETE=true
# BIND_KEY_API_ZONE_CLEANUP_STRATEGY=enumerate
# Leave freeze off (default) to avoid nsupdate REFUSED while zone is frozen; allow AXFR for full lists.
# BIND_KEY_API_FREEZE_ZONE_BEFORE_CLEANUP=false
# BIND_KEY_API_ZONE_CLEANUP_ENUMERATE_VIA_AXFR=true
